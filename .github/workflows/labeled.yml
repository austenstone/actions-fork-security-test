name: CI

on:
  pull_request:
    types: [labeled, synchronize]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - run: echo Hello, world!
